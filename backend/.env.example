# Database
DATABASE_URL=postgresql://user:password@localhost:5432/penta

# JWT secret (change to a strong random value in production)
TOKEN_SECRET=change_me_to_a_long_random_string

# App
NODE_ENV=development
PORT=3000
